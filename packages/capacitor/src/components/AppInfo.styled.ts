import styled from 'styled-components'

const AppInfoContainer = styled.div`
  position: absolute;
  bottom: max(var(--safe-area-inset-bottom), 20px);
  display: flex;
  flex-direction: column;
`

export { AppInfoContainer }
